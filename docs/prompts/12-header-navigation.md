# Prompt 12: Навигация Header

Обнови `src/components/layout/site-header.tsx`, сохранив текущий бренд ЗаказАвто25 и CTA.

Добавь маршруты/якоря: главная, преимущества, вопросы (`/#faq`), отзывы (`/#reviews`), контакты (`/#contacts`), автомобили и кейсы.

Требования:
- Не ломай навигацию на внутренних страницах: якоря главной должны начинаться с `/`.
- Проверь мобильное меню: если кнопка пока неработающая, реализуй доступное раскрытие или явно не показывай ложный интерактивный control.
- Добавь `aria-label` и видимый focus.
- Не добавляй ссылку на аукционы без подтвержденной бизнес-необходимости.

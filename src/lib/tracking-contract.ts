type TrackingOrder = { orderNumber: string; status: string; createdAt: Date; pricingPublic: boolean; vehiclePrice: unknown; currency: string | null; currencyRate: unknown; totalCost: unknown; shippingCost: unknown; customsCost: unknown; utilisationFee: unknown; brokerageCost: unknown; otherCosts: unknown; commission: unknown; vehicle: { make: string; model: string; year: number; media: { url: string }[] } | null; timeline: { id: string; createdAt: Date; title: string; description: string | null; type: string }[] };

export function serializeTrackingOrder(order: TrackingOrder) {
  const pricing = order.pricingPublic && order.totalCost !== null ? { vehiclePrice: order.vehiclePrice, currency: order.currency, currencyRate: order.currencyRate, vehiclePriceRub: order.vehiclePrice && order.currencyRate ? Number(order.vehiclePrice) * Number(order.currencyRate) : null, shippingCost: order.shippingCost, customsCost: order.customsCost, utilisationFee: order.utilisationFee, brokerageCost: order.brokerageCost, otherCosts: order.otherCosts, commission: order.commission, totalCost: order.totalCost } : null;
  return { orderNumber: order.orderNumber, status: order.status, vehicle: order.vehicle ? { make: order.vehicle.make, model: order.vehicle.model, year: order.vehicle.year, photo: order.vehicle.media[0]?.url ?? null } : null, pricing, timeline: order.timeline, createdAt: order.createdAt };
}
